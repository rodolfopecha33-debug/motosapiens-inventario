import React,{useState} from "react";import data from "./data";export default function Inventario(){const[q,setQ]=useState("");const f=data.filter(x=>x.nombre.toLowerCase().includes(q.toLowerCase()));return <div className="wrap"><h2>Inventario</h2><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar producto..." className="search"/><div className="grid">{f.map((p,i)=><div key={i} className="card"><b>{p.nombre}</b><small>{p.categoria}</small><span>${p.precio}</span><em>Stock {p.stock}</em></div>)}</div></div>}
